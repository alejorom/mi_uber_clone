import 'package:flutter/material.dart';

class TextoTituloRol extends StatelessWidget {
  const TextoTituloRol({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text(
      'SELECCIONA TU ROL',
      style: TextStyle(color: Colors.white, fontSize: 20),
    );
  }
}
